# Migration `20200509185849-update-schema`

This migration has been generated by alvarotorresc at 5/9/2020, 6:58:49 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Rol" AS ENUM ('Admin', 'Researcher');

CREATE TYPE "Types" AS ENUM ('APPARITION', 'PSYCHOPHONY', 'HAUNTED_HOUSE', 'REINCARNATION', 'TELEPATHY', 'TELEKINESIS', 'UFOLOGY');

CREATE TABLE "public"."DisabledToken" (
"expiresAt" timestamp(3)  NOT NULL ,"token" text  NOT NULL ,
    PRIMARY KEY ("token"))

CREATE TABLE "public"."Researcher" (
"age" integer  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"firstName" text  NOT NULL ,"id" text  NOT NULL ,"image" text  NOT NULL ,"lastName" text  NOT NULL ,"nationality" text  NOT NULL ,"password" text  NOT NULL ,"rol" "Rol" NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Phenomena" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text  NOT NULL ,"id" text  NOT NULL ,"researcherId" text  NOT NULL ,"title" text  NOT NULL ,"type" "Types" NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Ocurrence" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date" timestamp(3)  NOT NULL ,"description" text  NOT NULL ,"id" text  NOT NULL ,"phenomenaId" text  NOT NULL ,"resolved" boolean  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"witness" boolean  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Researcher.email" ON "public"."Researcher"("email")

ALTER TABLE "public"."Phenomena" ADD FOREIGN KEY ("researcherId")REFERENCES "public"."Researcher"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Ocurrence" ADD FOREIGN KEY ("phenomenaId")REFERENCES "public"."Phenomena"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200508192907-update-phenomena..20200509185849-update-schema
--- datamodel.dml
+++ datamodel.dml
@@ -3,11 +3,16 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
+model DisabledToken {
+  token     String   @id
+  expiresAt DateTime
+}
+
 enum Rol {
   Admin
   Researcher
 }
@@ -45,9 +50,9 @@
   updatedAt    DateTime    @updatedAt
   createdAt    DateTime    @default(now())
   researcher   Researcher  @relation(fields: [researcherId], references: [id])
   researcherId String
-  ocurrences   Ocurrence[]
+  ocurrence    Ocurrence[]
 }
 model Ocurrence {
   id          String    @default(uuid()) @id
```


