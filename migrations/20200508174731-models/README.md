# Migration `20200508174731-models`

This migration has been generated by alvarotorresc at 5/8/2020, 5:47:31 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Types" AS ENUM ('APPARITION', 'PSYCHOPHONY', 'HAUNTED_HOUSE', 'REINCARNATION', 'TELEPATHY', 'TELEKINESIS', 'UFOLOGY');

CREATE TABLE "public"."Phenomena" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"description" text  NOT NULL ,"id" text  NOT NULL ,"title" text  NOT NULL ,"type" "Types" NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Ocurrence" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"date" timestamp(3)  NOT NULL ,"description" text  NOT NULL ,"id" text  NOT NULL ,"resolved" boolean  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"witness" boolean  NOT NULL ,
    PRIMARY KEY ("id"))
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200508174231-researcher..20200508174731-models
--- datamodel.dml
+++ datamodel.dml
@@ -3,16 +3,26 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Rol {
   Admin
   Researcher
 }
+enum Types {
+  APPARITION
+  PSYCHOPHONY
+  HAUNTED_HOUSE
+  REINCARNATION
+  TELEPATHY
+  TELEKINESIS
+  UFOLOGY
+}
+
 model Researcher {
   id          String   @default(uuid()) @id
   email       String   @unique
   password    String
@@ -23,5 +33,24 @@
   image       String
   rol         Rol
   updatedAt   DateTime @updatedAt
   createdAt   DateTime @default(now())
+}
+
+model Phenomena {
+  id          String   @default(uuid()) @id
+  title       String
+  description String
+  type        Types
+  updatedAt   DateTime @updatedAt
+  createdAt   DateTime @default(now())
+}
+
+model Ocurrence {
+  id          String   @default(uuid()) @id
+  date        DateTime
+  description String
+  witness     Boolean
+  resolved    Boolean
+  updatedAt   DateTime @updatedAt
+  createdAt   DateTime @default(now())
 }
```


